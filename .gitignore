# raw downloads (large, re-fetchable)
data-raw/

# large regenerable training tables (rebuild via ml/build_features.py)
backend/data/flood_dataset.parquet
backend/data/drought_dataset.parquet

# python
backend/venv/
__pycache__/
*.pyc
.pytest_cache/

# node
frontend/node_modules/
frontend/dist/

# editor/os
.vscode/
.DS_Store
Thumbs.db

# deployment build artifacts
backend/app/static/
backend/.venv-prod/

# runtime logs from a locally started server
*.log

# seeded on first run by app/routers/auth.py, and a credential file has no
# business in a repository even as a hash
backend/data/users.json

# written by operators at runtime, not source
backend/data/decisions.jsonl
