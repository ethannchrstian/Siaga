# Serving dependencies only. Everything here is imported by app/ at runtime.
#
# Deliberately excludes geopandas/rasterio/shapely (GDAL toolchain), xgboost and
# scikit-learn. Those are needed to rebuild the datasets and retrain the models,
# not to serve them: predictions are precomputed into data/risk_history.parquet
# and centroids into data/district_centroids.csv. Keeping them out takes the
# container build from several minutes with a common failure mode down to under
# a minute. For that work install requirements-dev.txt instead.
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4
pandas==3.0.5
pyarrow==25.0.0
numpy==2.5.1
PuLP==3.3.2
